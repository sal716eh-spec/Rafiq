name = "rafiq-judge"
main = "src/index.js"
compatibility_date = "2026-09-01"

# The TypeSafe key is a secret, never in this file:
#   Cloudflare dashboard → Workers → rafiq-judge → Settings → Variables and Secrets
#   → add secret TYPESAFE_API_KEY

# Needed for Cloudflare's preview builds of non-main branches ("npx wrangler
# preview"); an empty block is enough. Production deploys from main ignore it.
[previews]
